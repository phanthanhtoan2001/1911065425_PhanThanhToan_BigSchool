
@model _1911065425_PhanThanhToan_BigSchool.Models.Course
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2>Create</h2>
<form>
    <div class="form-group">
        @Html.LabelFor(m => m.Place)
        @Html.TextBoxFor(m => m.Place, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Leturer)
        @Html.TextBoxFor(m => m.Leturer, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.DateTime)
        @Html.TextBoxFor(m => m.DateTime, new { @class = "form-control" })
    </div>
    
    <button type="submit" value="Save" class="btn btn-primary">Save</button>
    <div class="form-group"></div>
    <div class="form-group"></div>
</form>